# setup.py

from setuptools import setup, find_packages

setup(
    name='my_python_project',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # Your project's dependencies would go here.
        # For this example, we'll keep it simple.
    ],
    # You can add more metadata here, like author, description, etc.
)
